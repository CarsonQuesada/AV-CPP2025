uint8  profile_id        # 0=DEFAULT, 1=LANES_ONLY, 2=OBSTACLES_ONLY, ...
uint8  features_mask     # bit flags to mix features (e.g., 1=LANES, 2=OBSTACLES, 4=SPEED_CTRL, ...)
string[] keys            # optional override names
float64[] values         # same length as keys; numeric overrides
