drive_arbiter_node:
  ros__parameters:
    vehicle.top_speed_mps: 13.0     # <— use this key; remove/ignore v_max_mps
    steer.max_rad: 0.5
    override.deadband_percent: 2    # only if your node declares this name
    publish_when_inactive: false    # only works if the node declares it
