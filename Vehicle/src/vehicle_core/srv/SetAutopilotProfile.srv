# Request: you can set any combination; unset fields keep current
bool   set_profile_id
uint8  profile_id

bool   set_features_mask
uint8  features_mask

# Optional overrides; both arrays must be the same length if provided
bool     set_params
string[] keys
float64[] values
---
bool success
string message
